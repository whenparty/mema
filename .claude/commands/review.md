Use the reviewer agent to review current uncommitted changes.

If $ARGUMENTS is provided, use it as context for what was implemented
(e.g., task ID or description).

If no arguments, infer context from git diff and recent commits.
