---
description: Apply project testing conventions including required Docker-based e2e gate.
globs:
  - "src/**/*.ts"
  - "tests/**/*.ts"
alwaysApply: false
---

- Follow TDD workflow where practical: failing test, implementation, refactor.
- Tests should verify behavior, not implementation details.
- Unit tests live under module `tests/` directories.
- E2E tests live in `tests/e2e/` and must be black-box.
- For feature-complete changes, ensure local Docker e2e execution is run and reported.
- If e2e fails, do not mark task complete; report failures with file and test names.
- Use only canonical package scripts from `package.json` (e.g., `test:unit`, `test:e2e`, `test:e2e:docker`), not undocumented aliases.
- E2E preflight must include DB reachability from host test process and `DATABASE_URL` validity.
- `bun run test:e2e` is the raw suite entrypoint for environments where DB reachability is already prepared (e.g. CI service containers).
- For local end-to-end coverage, prefer docker-backed `bun run test:e2e:docker`.
